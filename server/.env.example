# LeagueStats Coach API - Environment Variables
# Copy this file to .env and fill in the values

# ========================================
# DATABASE
# ========================================
# PostgreSQL connection string from Neon (https://neon.tech)
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxx-xxx.us-east-2.aws.neon.tech/leaguestats?sslmode=require

# ========================================
# API CONFIGURATION
# ========================================
# API host and port (development)
API_HOST=0.0.0.0
API_PORT=8000

# CORS allowed origins (comma-separated)
# Development: http://localhost:3000,http://localhost:5173
# Production: https://leaguestats-web.vercel.app
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ========================================
# CELERY & REDIS (Phase 2 - Worker)
# ========================================
# Redis connection URL (Upstash free tier recommended)
# REDIS_URL=redis://default:password@host:port

# ========================================
# LOGGING
# ========================================
LOG_LEVEL=INFO

# ========================================
# ADMIN API
# ========================================
# Admin API key for internal operations (force DB pool refresh)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# MUST match between local .env and Render dashboard
ADMIN_API_KEY=your-secure-api-key-here

# ========================================
# RENDER DEPLOYMENT (Production)
# ========================================
# These env vars are set automatically by Render
# Do NOT commit to git - set in Render dashboard
# - DATABASE_URL
# - REDIS_URL (Phase 2)
# - PORT (automatically injected by Render)
