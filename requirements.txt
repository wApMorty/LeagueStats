# LeagueStats Coach - Dependencies
# Generated: 2025-11-27

# Web scraping and automation
selenium>=4.15.0,<5.0.0
lxml>=5.1.0,<6.0.0

# Data processing and analysis
# Note: NumPy 2.x required for Python 3.13 (pre-built wheels available)
numpy>=2.0.0,<3.0.0

# Database - PostgreSQL async (Sprint 2 - Neon PostgreSQL Integration)
sqlalchemy[asyncio]==2.0.46  # Async SQLAlchemy with asyncio support
asyncpg==0.31.0  # Async PostgreSQL driver
greenlet>=3.0.0  # Required for SQLAlchemy async support

# HTTP requests
requests>=2.31.0,<3.0.0
urllib3>=2.0.0,<3.0.0
httpx>=0.27.0,<1.0.0  # Async HTTP client for API data source (Sprint 2 - API Integration)

# System and process management
psutil>=5.9.6,<6.0.0

# Environment variables (.env file loading)
python-dotenv>=1.0.0,<2.0.0

# Retry mechanism for robust web scraping (Sprint 2)
tenacity>=8.2.0,<10.0.0

# Progress bars for user feedback (Sprint 2)
tqdm>=4.66.0,<5.0.0

# Windows notifications for auto-update (Sprint 2 - Tâche #11)
# Note: Replaced win10toast (Python 2 dependency) with windows-toasts (Python 3.13 compatible)
# Windows-only dependency: install with pip install -r requirements.txt only on Windows
windows-toasts>=1.0.0,<2.0.0; sys_platform == 'win32'

# Build tool (optional - only needed for creating executable)
# pyinstaller>=6.0.0,<7.0.0
