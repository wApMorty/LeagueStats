# LeagueStats Coach - Environment Variables Template
# Copy this file to .env and fill in your actual values

# Admin API Key (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
# This key must match between local .env and server/.env (and Render dashboard)
ADMIN_API_KEY=your-secure-api-key-here
